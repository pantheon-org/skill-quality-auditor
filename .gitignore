dist/
coverage.out
coverage.html
.aislop/
.claude/skills
.codex/
.cursor/
.github/skills
.tessl/
.env

# Override the global ~/.gitignore `.agents/` exclusion so local skills are tracked.
# Must un-ignore parent dirs first — git won't descend into an excluded directory.
!.agents/
!.agents/skills/
.agents/skills/tessl__*
!.agents/skills/context-file/
!.agents/skills/context-index/