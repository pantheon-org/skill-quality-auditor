{
  "checklist": [
    { "description": "Never assume trust without verifying the call chain", "max_score": 50 },
    { "description": "Always validate input before any mutation is applied", "max_score": 50 }
  ]
}
